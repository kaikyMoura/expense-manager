//import { useRouter } from "next/router"


interface NavegacaoProps {
    para : any
}

const Navegacao = ({para}: NavegacaoProps) => {
    
    //const router = useRouter()
}

export default Navegacao